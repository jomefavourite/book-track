import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.favouritejome.booktrackr',
  appName: 'Book-Trackr',
  webDir: 'out'
};

export default config;
